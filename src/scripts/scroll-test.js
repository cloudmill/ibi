import swipeDetect from 'swipe-detect'

window.addEventListener('DOMContentLoaded', () => {
  const scrollTest = document.querySelector('.scroll-test')

  if (scrollTest) {
    const screens = document.querySelectorAll('.scroll-test__screen')

    let index
  }
})
