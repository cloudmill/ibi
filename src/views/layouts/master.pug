include ../components/icons

include ../components/_header
include ../components/_footer

include ../components/grid-test

block vars

doctype html

html(lang='ru')

  head
    meta(charset="UTF-8")

    block title
      title ibi

    <!--[if IE]>
    meta(http-equiv='X-UA-Compatible', content='IE=edge,chrome=1')
    <![endif]-->

    meta(name="viewport", content="width=device-width, initial-scale=1")

    meta(name="format-detection", content="telephone=no")
    meta(name="format-detection", content="address=no")

    link(rel="icon", type='image/png' href="assets/images/favicon-16x16.png" sizes='16x16')
    link(rel="icon", type='image/png' href="assets/images/favicon-32x32.png" sizes='32x32')
    link(rel="shortcut icon" href="assets/images/favicon.ico")

    link(rel="apple-touch-icon" href="assets/images/svg/appletouch_180_180.svg")

    meta(property='og:image' content='assets/images/opengraph.jpg')
  
  body.body(class=[isSeq ? 'body--seq' : undefined] data-mobile-seq=isSeq)
    block preloader

    block grid-test
      +grid-test
    
    if (isSeq)
      
      .expand
        .expand__scroll
          .expand__content
       
            .wrapper(class=[isSeq ? 'wrapper--seq' : undefined])
        
              block header
                +header
                
              main.main
                block content

              block footer
                +footer

              include ../components/modal-vacancy
              include ../components/modal-callback

              include ../components/modal-tooltip-1
              include ../components/modal-tooltip-2

             
              
    else

      .wrapper(class=[isSeq ? 'wrapper--seq' : undefined])
        
        block header
          +header
          
        main.main
          block content

        block footer
          +footer

        include ../components/modal-vacancy
        include ../components/modal-callback

        include ../components/modal-tooltip-1
        include ../components/modal-tooltip-2

    block scripts
      //- script(src="https://www.youtube.com/iframe_api")

